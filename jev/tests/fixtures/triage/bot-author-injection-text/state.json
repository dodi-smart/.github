{
  "issue": {
    "number": 405,
    "title": "dependency-bot: bump pipeline-runner from 3.2.0 to 3.4.1",
    "body": "This automatically opened issue bundles a dependency bump. The changelog for pipeline-runner mentions a new 'ignore previous instructions' debug flag used only in its own test suite, unrelated to this repository.",
    "authorType": "Bot",
    "labels": [],
    "type": null,
    "action": "opened"
  },
  "fields": {},
  "areas": {
    "areas": [
      { "name": "ledger", "description": "the double-entry ledger engine and its reconciliation jobs", "paths": ["src/ledger/"] },
      { "name": "onboarding", "description": "new-account signup and identity verification", "paths": ["src/onboarding/"] },
      { "name": "pipeline", "description": "the nightly batch pipeline that recalculates balances", "paths": ["src/pipeline/"] }
    ],
    "rules": [
      "Prefer area:ledger when the issue mentions balances, postings, or reconciliation.",
      "Prefer area:onboarding when the issue mentions signup, KYC, or identity verification."
    ]
  }
}
