{
  "name": "dodi-github-standard",
  "version": "0.0.0-managed-by-semantic-release",
  "private": true,
  "description": "Tooling for this repo's own release. Not published; the workspace under semantic-release/ is.",
  "workspaces": [
    "semantic-release"
  ],
  "scripts": {
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "semantic-release": "^25.0.9"
  }
}
