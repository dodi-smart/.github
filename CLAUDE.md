# CLAUDE.md

See **[AGENTS.md](AGENTS.md)**. One source, so a change is made once rather than
twice with the second one forgotten.

One thing worth knowing before you read anything else. This repo is public and
every org repository it serves is private, so never name a product, repository,
customer, host or internal URL here. Not in code, not in a comment, not in a
commit message, not in a pull request description.
